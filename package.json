{
  "name": "pi-workspace-gate",
  "version": "0.0.1",
  "description": "Security gate for pi — blocks dangerous commands, sensitive file access, and paths outside the workspace",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "test": "pi -ne -ns -e ./extensions/pi-workspace-gate"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/goga-m/pi-workspace-gate"
  },
  "files": [
    "extensions"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
